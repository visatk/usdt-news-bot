{
  "name": "usdt-news-bot",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler types && wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "cf-typegen": "wrangler types",
    "db:generate": "drizzle-kit generate",
    "db:push": "npx wrangler d1 migrations apply usdt-news-bot --remote"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^5.5.2",
    "drizzle-kit": "^0.31.10",
    "wrangler": "^4.94.0"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "grammy": "^1.43.0"
  }
}
